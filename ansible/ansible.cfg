[defaults]
inventory = inventory.ini
host_key_checking = False
private_key_file = ../terraform/ec2_key.pem
remote_user = ubuntu
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
